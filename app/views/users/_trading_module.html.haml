.juv
  =link_to "/items/#{@item.id}/edit", class: "hanage" do
    .qq 商品の編集
  .qqq or
  - if @item.exhibition_state == "出品中"
    =link_to hoge_item_stop_item_path do
      .qqqq 出品を一旦中止する
  - else
    =link_to hoge_item_start_item_path do
      .qqqq 出品を再開する
  =link_to item_path ,method: "delete" do
    .qqqqq この商品を削除する